# Inputs Directory

This directory contains **input data** that the system uses (not generated artifacts).

## 📁 Structure

```
inputs/
├── web_voyager/           # WebVoyager benchmark tasks (643 curated)
│   └── web_voyager_data.jsonl
│
└── reward_model/          # Reward Model training data
    ├── ckpts/            # Model checkpoints
    │   ├── reward_model.pt
    │   ├── semantic_encoder.pt
    │   └── dom_event_baseline.pkl
    ├── features/         # Extracted features for training
    │   ├── train_*.jsonl
    │   ├── val_*.jsonl
    │   └── test_*.jsonl
    ├── pairs/            # Training pairs
    │   └── pairs.jsonl
    ├── splits/           # Train/val/test splits
    └── llm_labels/       # LLM-generated labels
```

## 🎯 Purpose

**Everything in this directory is:**
- ✅ Input data (not generated outputs)
- ✅ Relatively stable (doesn't change on every run)
- ✅ Can be version controlled (or downloaded separately)
- ✅ Required for the system to function

## 📊 vs outputs/

| Directory | Purpose | Examples | Changes Frequently? |
|-----------|---------|----------|---------------------|
| `inputs/` | **Input data** | Datasets, model checkpoints | ❌ No |
| `outputs/` | **Generated artifacts** | Benchmark results, logs | ✅ Yes |

## 📝 Contents

### **web_voyager/**
Reference dataset for benchmarking on real websites:
- 643 manually curated tasks for websites like Allrecipes, etc.
- Used by judge_benchmark for testing agents on production sites

### **reward_model/**
Data for training the reward prediction model:
- `ckpts/` - Trained model checkpoints (.pt, .pkl files)
- `features/` - Extracted features from browser snapshots
- `pairs/` - Positive/negative pairs for contrastive learning
- `splits/` - Dataset splits for train/val/test
- `llm_labels/` - Labels generated by LLM for supervision

---

## 🔄 Relationship with outputs/

```
inputs/                          outputs/
└── reward_model/               └── training/
    ├── ckpts/                      └── reward_model/
    │   └── reward_model.pt             ├── training_logs/
    └── features/                       └── evaluation_metrics/
        └── train_*.jsonl

        ↓                                   ↓
    (Input para training)              (Output de training)
```

The reward model uses `data/inputs/reward_model/` as training data,
and produces training logs/metrics in `outputs/training/reward_model/`.

---

## 🧹 Maintenance

**Safe to:**
- ✅ Version control these files (or download from shared storage)
- ✅ Back up for reproducibility
- ✅ Share with team

**Do NOT:**
- ❌ Delete without backup
- ❌ Regenerate frequently (expensive)
- ❌ Store temporary outputs here (use outputs/ instead)
