# Compatibility package for legacy imports.
# The data generation domain package was moved under `tasks`.
# Keep this module to avoid breaking downstream consumers in the subnet.

from .classes import *  # noqa: F403
